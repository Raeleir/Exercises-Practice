let defaultState = {
    item: ""
}

const mainReducer = (state = defaultState, action) => {
    if(action.type === "GEN_ITEM") {

    } else {
        return state;
    }
}

export default mainReducer;